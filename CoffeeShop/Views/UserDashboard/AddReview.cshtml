@using CoffeeShop.ViewModels
@model AddReviewViewModel
@{
    ViewData["Title"] = "Write a Review";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<div class="ud-page-header">
    <div class="ud-eyebrow">Reviews</div>
    <h1 class="ud-title">Write a <em>Review</em></h1>
</div>

<div style="max-width:680px;">
    <div class="ud-panel">
        <div class="ud-panel-header">
            <div class="ud-panel-title-grp">
                <div class="ud-panel-icon"><i class="fas fa-star"></i></div>
                <span class="ud-panel-title">@Model.ProductName</span>
            </div>
        </div>
        <div class="ud-panel-body">
            @if (TempData["Error"] != null)
            {
                <div class="u-alert error"><i class="fas fa-exclamation-circle"></i> @TempData["Error"]</div>
            }

            <form asp-action="AddReview" method="post">
                <input type="hidden" asp-for="ProductID" />
                <input type="hidden" asp-for="ProductName" />

                <!-- Rating -->
                <div class="u-form-group">
                    <label class="u-label">Your Rating *</label>
                    <div class="rating-input">
                        @for (int i = 5; i >= 1; i--)
                        {
                            <input type="radio" asp-for="Rating" value="@i" id="star@(i)" required />
                            <label for="star@(i)" title="@i stars"><i class="fas fa-star"></i></label>
                        }
                    </div>
                    <span asp-validation-for="Rating" class="u-validation"></span>
                </div>

                <!-- Comment -->
                <div class="u-form-group">
                    <label class="u-label">Your Review <span style="color:var(--muted);font-weight:400;text-transform:none;letter-spacing:0;">(Optional)</span></label>
                    <textarea asp-for="Comment" class="u-input" rows="5" placeholder="Share your thoughts about this product…" style="resize:vertical;"></textarea>
                    <span asp-validation-for="Comment" class="u-validation"></span>
                    <div style="font-size:11px;color:var(--muted);margin-top:5px;">Maximum 1000 characters</div>
                </div>

                <div style="display:flex;gap:10px;">
                    <button type="submit" class="u-btn u-btn-primary"><i class="fas fa-paper-plane"></i> Submit Review</button>
                    <a asp-controller="Products" asp-action="Detail" asp-route-id="@Model.ProductID" class="u-btn u-btn-outline">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <style>
        .rating-input {
            display: inline-flex;
            flex-direction: row-reverse;
            gap: 4px;
            margin-bottom: 6px;
        }

            .rating-input input[type="radio"] {
                display: none;
            }

            .rating-input label {
                cursor: pointer;
                font-size: 28px;
                color: rgba(181,117,42,0.2);
                transition: color 0.15s;
            }

                .rating-input label i {
                    pointer-events: none;
                }

                .rating-input input[type="radio"]:checked ~ label,
                .rating-input label:hover,
                .rating-input label:hover ~ label {
                    color: var(--caramel);
                }
    </style>
}
